services:
  webdroid-agent:
    build:
      context: .
    image: webdroid-agent:latest
    container_name: webdroid-agent
    restart: unless-stopped
    ports:
      - "8083:8080"
    environment:
      NODE_ENV: production
      PORT: 8080
